// Fixture Rust file for @vscode/test-electron runtime tests.
// Two TODO(@user) markers in line comments. The extension should
// detect both as DocumentLinks and produce a Hover at either.
// (ASCII-only on purpose: providers.ts compares VSCode UTF-16
// offsets against engine UTF-8 byte offsets; non-ASCII content
// throws off the lookup. Tracked in docs/test-plan.md.)

// TODO(@alice) review this fixture before publish
fn main() {
    println!("hello");
}

// TODO(@bob) update the regex if the field set changes
