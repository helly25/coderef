# Rust build artifacts
target/
Cargo.lock.bak
**/*.rs.bk

# TypeScript / VSCode-extension build artifacts
out/
dist/
*.vsix
*.tsbuildinfo

# Node dependencies (committed lockfiles, not the trees)
node_modules/
npm-debug.log
yarn-error.log

# Test runners
.vscode-test/
coverage/

# Coderef runtime / cache
.coderef-cache/
.coderef/

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
*.swp

# Research / discussion log — internal-only for now.
# May be promoted to a tracked doc when the project ships.
RESEARCH.md
