# docs/

Long-form documentation that doesn't belong in the working spec
(`../DESIGN.md`) or in the README.

Empty at v0.0.0. Future contents per the v0.4 roadmap include:

- `integrations/<ci-platform>.md` — recipes for CI systems beyond the
  ones shown in `DESIGN.md` §16.4.1–§16.4.3.
- `examples/` walkthroughs.
- A migration guide for users coming from `tagref` / `lychee` /
  `todocheck` / Chromium-style `LINT.IfChange`.
